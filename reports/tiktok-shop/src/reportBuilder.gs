// ============================================================
// REPORT BUILDER — Tổng hợp & hiển thị báo cáo
// ============================================================

function buildReport(sheetData, bqData) {
  // TODO: Xây dựng logic báo cáo sau khi có đủ thông tin schema
  Logger.log("Sheet rows: " + sheetData.length);
  Logger.log("BQ rows: " + bqData.length);
}
